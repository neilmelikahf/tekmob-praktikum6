import 'package:flutter/material.dart';

class AppColors {
  // Elegant pink palette (rosy primary, soft blush accents)
  static const Color primary = Color(0xFFD63384); // rosy pink
  static const Color secondary = Color(0xFFF8CFEA); // light blush
  static const Color accent = Color(0xFFFFE6F3); // pale highlight
  static const Color success = Color(0xFF16A34A); // green for success
  static const Color surface = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFFFF7FB); // very light pink
  static const Color muted = Color(0xFF6B7280);
  static const Color danger = Color(0xFFEF5350);
  // Text & UI helpers
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF374151);
  static const Color placeholder = Color(0xFF9CA3AF);
  static const Color border = Color(0xFFD1D5DB);
  static const Color placeholderBackground = Color(0xFFF3F4F6);
}
